import React from 'react';

const Navbar: React.FC = () => (
  <nav className="w-full h-14 bg-red-800 flex items-center px-4 text-white shadow">
    <span className="font-bold text-lg">RedOak</span>
  </nav>
);

export default Navbar;
